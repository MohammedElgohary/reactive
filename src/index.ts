/**
 * Reactive — lightweight, framework-agnostic reactive state management
 */

export {
  // Core
  reactive,
  computed,
  computedAsync,
  effect,
  batch,
  isBatchingUpdates,
  scheduleNotification,
  readonly,
  readonlyObject,
  watch,
  watchMultiple,
  watchProperty,
  // Utilities
  toRaw,
  markRaw,
  isRaw,
  shallowReactive,
  isReactive,
  isComputed,
  // Debug
  setDebug,
  isDebugEnabled,
  trackReactive,
  getDebugInfo,
  logTrackedReactive,
  clearDebugTracking,
  // DOM bindings
  bindText,
  bindHTML,
  bindAttr,
  bindProp,
  bindClass,
  bindStyle,
  bindStyles,
  bindInput,
  bindMultiple,
  bind,
  render,
  // Events
  bindAction,
  onClick,
  onDblClick,
  onInput,
  onChange,
  onSubmit,
  onKeyDown,
  onKeyUp,
  onFocus,
  onBlur,
  onMouseEnter,
  onMouseLeave,
  onScroll,
  onKey,
  onEnter,
  onEscape,
  // Security utilities
  escapeHtmlEntities,
  sanitizeHtmlContent,
  isUrlSafe,
  configureReactiveSecurity,
} from "./core";

export type {
  Reactive,
  Computed,
  ComputedWithDispose,
  AsyncComputed,
  AsyncComputedOptions,
  EffectFn,
  EffectCleanup,
  EffectOptions,
  Unsubscribe,
  WatchSource,
  WatchCallback,
  WatchOptions,
  BindingSource,
  BindingOptions,
  MultiBinding,
  ActionOptions,
  ActionHandler,
  KeyFilter,
} from "./core";
